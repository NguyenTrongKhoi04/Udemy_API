/**
 * * SOAP:
 *      ? wsdl=http://www.dneonline
 *      ? Mặc định phương thức là POST
 *      ? Tính chặt chẽ cao hơn > bảo mật cao > dùng trong môi trường bảo mật (nội bộ, ...)
 *      ? Liên quan đến XML
 * * REST 
 *      ? 4 method: GET POST PUT DELETE
 *      ? Không yêu cầu nhiều quy tắc
 *      ? Liên quan đến JSON
 */